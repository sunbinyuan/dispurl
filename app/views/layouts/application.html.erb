<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Disposable URL</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	<nav class="top-bar" data-topbar role="navigation">
	  <ul class="title-area">
	    <li class="name">
	      <h1><a href="/">Disposable URL</a></h1>
	    </li>
	     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
	    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
	  </ul>
	
	  <section class="top-bar-section">
	    <!-- Right Nav Section -->
	    <ul class="right">
	    <% if user_signed_in? %>
        <li><a>Signed in as: <%= current_user.email%></a></li>	    
        <li><%= link_to "Log out", destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' } %></li>
	    <% else %>
        <li><%= link_to "Log in", new_user_session_path %></li>
	    <% end %>
	    </ul>
	
	    <!-- Left Nav Section -->
	    <ul class="left">
	    <% if user_signed_in? %>
        <li><%= link_to "My URLs", urls_path %></li>
	    <% end %>
	    </ul>
	  </section>
	</nav>
	<% if notice %>
   <div data-alert class="alert-box info"><%= notice %></div>
  <% end %>
  <% if alert %>
   <div data-alert class="alert-box"><%= alert %></div>
  <% end %>
  <br>
<%= yield %>
  
<div class="footer">
  <div class="row">
    <p class="copyright">© 2015 sunbinyuan. All rights reserved.</p>
  </div>
</div>

</div>
<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
</body>
</html>
